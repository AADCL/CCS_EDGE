# UGV_004 修复部署记录（2026-09-24）

## 范围与根因

远程基线 main `8196988c86236982ae35b05205c8d5fbffb5194b`；分支 `codex/ugv004-camera-localization-navigation`。原工作区未改动。

设备日志显示初始 NDT 为 `WAITING_ALLOWED_AREA regions=0`，上层却将默认 TF 报为 localized；move_base 随后报 map/base_link 分离，25 秒准备超时。任务自动准备重试及查询缺少错误上下文造成重复 received。原 profile 明确禁用视频，未启动已安装 Gemini 驱动。

## 部署边界与版本

只写 `/home/nrc15/ccs_edge_ws`。只更新重定位 0.5.0 → 0.6.0、任务控制 0.6.3 → 0.6.4 及 UGV_004 相关配置/脚本。视频包沿用仓库已有 0.1.2，下午修正时将端侧旧 0.1.1 源码和二进制更新至该版本；仓库不另行提升版本。设备现有 MQTT 0.4.1、UDP 0.3.1 及对应旧 schema 配置保留；不随 main 的遥测迁移升级。

受影响文件：`src/EPGeneral_relocalization/**`、`src/EPGeneral_task_control/**`；`config/wheeltec_r550p_02/{relocalization,task_control,video}.yaml`；根 `start_ccs_edge_dev.sh`；`scripts/{ccs_sntp_sync,ccs_wheeltec_preflight,ccs_camera_supervisor}.py`；`launch/wheeltec_r550p_02_bringup.launch`；CCS 的 build/devel 编译产物。地图及任务原始数据保留。

备份：`backups/ugv004-fix-20260924/before.tar.gz`，SHA-256 `7f7446f637e06980c116aa838ae844091261c353d4f195d448409ab72c08ec14`。原生 src 与 devel/lib 文件哈希在部署前记录；完成后复核。依赖源码原始哈希和许可证见包内 `wheeltec/NOTICE.md`。

## 验证记录

- 37 项定位测试、115 项任务控制测试及 12 项 profile/时间同步测试通过（合计 164）。C++ 门控测试通过，覆盖无区域初始连续确认、自动修正暂停、收敛/fitness/跳变拒绝、区域原子替换及清空。
- CCS 内可选 C++ 目标编译成功；启动预检查通过。时钟偏差约 -0.086 秒，限制 0.5 秒。
- 实机相机 60.41 秒：RGB 和深度各 1810 帧，五个必需业务节点持续存在。平台 ffprobe 实际解码 SRT 为 H.264、640×480、30/1 FPS；当时只验证了旋转配置，实际旋转在下述下午修正后重新验证。
- 重复根脚本启动返回 1，提示 already running。停止本次相机后，再观察 60.30 秒，RGB/深度无新帧但五个必需业务节点仍存在；根日志明确报告视频降级。
- 外部 RGBD 实例已被监督程序复用（29.5 FPS）。完整 60 秒复用及停止所有权观察被另一套流程注册同名底盘/雷达节点打断：base.log 为 new node registered with same name，随后必需驱动退出，根脚本正常清理。未干预外部流程；该项仍待独占验收。
- 无区域实机原点初始位姿被 NDT 正确拒绝（fitness 约 4.2–4.8，门限 2.0），未报告 localized。未放宽门限，也未注入假 TF。当前地图上的初始定位成功、区域真实应用及导航 ready 的 60 秒验收尚待正确当前位姿和独占设备时间；不宣称已通过。全程未发送 SCHEDULE 或行驶目标。

## 回滚

先向 `run/managed/startup.pid` 对应且命令匹配的根脚本发送 SIGINT，等待其按所有权停止相机、任务和底盘；确认无 CCS 任务运行。校验上述备份 SHA-256，在 CCS 内恢复备份包含的 src/config/launch/scripts/根脚本/build/devel。此次新增文件按部署清单单独移入 CCS 内隔离目录，避免残留可选目标。不要删除地图、mission、备份目录或修改原生工作空间。恢复后执行 `./start_ccs_edge_dev.sh --check`，按需要启动。备份中的旧时间同步修复会一并保留。


## 原生运行日志与外部并发变化

原生 FAST-LIO 即使关闭 runtime_pos_log_enable，仍会以写模式打开编译时固定的 Log 路径。首次实机检查识别此行为后停止栈；UGV_004 的 fastlio stage 现用设备已安装的 bubblewrap，以只读根文件系统、CCS 可写目录和 Log/PCD 到 CCS run 目录的绑定运行原生程序。没有修改原生源码、配置或二进制来解决此问题。

部署前清单包含 1091 个原生 src/devel 文件。中途复核发现 `fast_lio/Log/mat_pre.txt` 的运行日志变化；同时原生定位器源码、README、launch、二进制及消息 pyc 出现本任务未写入的外部变化。本任务不覆盖或回滚这些变化，因而不能宣称整份外部清单完全未变。原始清单与差异保存在 CCS 的 `backups/ugv004-fix-20260924/`，以及本地部署证据目录。

平台关闭时 UDP 123 无监听，一键启动正确拒绝运行；复用平台 NtpServerService 启动临时静止验收服务后，偏差约 0.016 秒，所有必需节点启动成功。验收工具关闭后，用户正常启动仍须先运行指控平台时间服务。


## RGB 启动与倒装旋转修正（2026-09-24 下午）

现场日志显示驱动已成功打开 Gemini 336L，RGB/深度均报告 30 FPS；失败来自监督脚本在启动半秒内按回调抵达间隔计算出 31.6/49.8 FPS，误判并停止了驱动。改为直接调用 `roslaunch orbbec_camera gemini_330_series.launch`，显式传入尺寸、帧率和禁用识别/点云参数；就绪判断使用图像采集时间戳，要求至少两秒的实际接收窗口，启动积压或瞬时抖动继续等待，最长 30 秒。新增四项回归覆盖突发帧、重复/陈旧帧、真实 60 FPS 及驱动参数，相关测试合计 16 项通过。

端侧实装视频包此前为 0.1.1，不支持 rotation_degrees；仅配置 180 不能让旧二进制旋转。现部署仓库已有的 0.1.2 并仅在 CCS 内编译视频目标。驱动 color/depth_rotation=0，串流 rotation_degrees=180，避免双重翻转；静态安装 TF 沿用原倒装标定。视频源码没有新增变更，不另造版本号。

本次备份 `backups/ugv004-fix-20260924/before-camera-fix-1438.tar.gz`，SHA-256 `87d56025c44332d7a217eea46579fa00e7d6e3e055b924df9b422f8076dcca91`。新增部署范围为 CCS 内 `src/EPGeneral_video_srt` 及其编译产物。临时恢复的相机监督进程关联当前根脚本 PID/启动标识，根脚本退出后会清理自己拥有的相机/视频子进程。

修正后连续观察 60.07 秒：RGB 1799 帧、深度 1800 帧，两者采集时间戳统计均为 29.96 FPS。SRT 实际解码 H.264、640×480、30/1 FPS；将原始 RGB 与解码图像缩小到同尺寸比较，未旋转平均像素差 76.47，旋转 180° 后为 15.18，确认倒装画面在串流端实际转正。原始图像话题保持未旋转。证据保存在 CCS 备份目录的 camera_rotation_validation.json、rotation_comparison.json、srt_rotation_probe.json；部署清单为 camera_deployment_manifest.json。根脚本及其他业务未重启。

本次相机回滚仅恢复 before-camera-fix-1438.tar.gz 内对应文件及视频二进制，先按上述所有权流程停止 CCS 相机/视频进程；不要使用更早的全栈备份覆盖本次之前的定位及任务修复。
